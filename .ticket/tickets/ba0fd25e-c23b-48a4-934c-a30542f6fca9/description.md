# Documentation

- Add a top-level note in `tools/viewer/viewer-api/README.md` linking to
  the demo-viewer and explaining its role as the canonical reference
  consumer.
- Author `tools/viewer/viewer-api/examples/demo-viewer/README.md` with:
  - How to run (prepare + start commands).
  - Feature inventory table linking to each spec.
  - How to add a new feature page (template).
- Update `agents/README.md` (if applicable) to mention the demo-viewer
  for new agent onboarding.
